# Local credentials — never commit a configured mcp.json with real secrets
mcp/mcp.json.local
*.local

# IDE settings
.vscode/settings.json
.vscode/*.code-workspace
.idea/
*.swp
*.swo
*~

# Claude Code local config
.claude/settings.local.json

# OS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary and log files
*.log
*.tmp

# Python bytecode / test caches (the hooks are Python; running tests/ creates these)
__pycache__/
*.py[cod]
.pytest_cache/
